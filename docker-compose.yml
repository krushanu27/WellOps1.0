services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: wellops
      POSTGRES_USER: wellops
      POSTGRES_PASSWORD: wellops_dev_pw
    ports:
      - "5432:5432"
    volumes:
      - wellops_pg:/var/lib/postgresql/data

volumes:
  wellops_pg:
